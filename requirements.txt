# SOUL — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core server ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.0
websockets==12.0
httpx==0.27.0
pydantic==2.7.0

# ── Perception ───────────────────────────────────────────────────────────────
Pillow==10.4.0
psutil==5.9.8

# ── TTS (text-to-speech) ─────────────────────────────────────────────────────
pyttsx3==2.90

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── v2 Voice Pipeline — uncomment to enable ──────────────────────────────────
# STT: local Whisper transcription (no cloud, no cost)
# openai-whisper
#
# Mic input: requires PortAudio system library
#   Windows:  pipwin install pyaudio  (if pip install fails)
#   macOS:    brew install portaudio && pip install pyaudio
#   Linux:    sudo apt install portaudio19-dev && pip install pyaudio
# pyaudio==0.2.14
#
# Wake word: Porcupine (requires PORCUPINE_KEY in .env)
#   Free key at: https://console.picovoice.ai
# pvporcupine==3.0.2
